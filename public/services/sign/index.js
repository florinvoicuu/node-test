/*
'use strict';

angular.module('app')
.config(function ($routeProvider) {
    $routeProvider
        .when('/', {
            controller: '',
            templateUrl:''
        })
        .when('/signup', {
            controller:'SignCtrl',
            templateUrl:'views/sign/index.html as user'
        })
        .when('/signin', {
            controller: 'SignCtrl',
            templateUrl:'views/sign/index.html as user'
        })
        .otherwise({
            redirectTo:'/'
        });
})*/
